#include<stdio.h>
/* writea program to check whether a given number is disible by 5 or not?*/
int main()
{
int number=56;
  if(number%5==0)
  printf("this is divisible by 5");
  else
  printf("this is not divisible by 5");
    return 0;
}