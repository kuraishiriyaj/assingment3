#include<stdio.h>
/* write  a program to check whether a given number is an even number
  or an odd number?
*/
int main()
{
  int number=5;
  
  if(number%2==0)
   printf("number is the even: %d",number);
   else
   printf("number is the odd: %d",number);
   
    return 0;
}